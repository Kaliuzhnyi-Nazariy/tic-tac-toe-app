import updateText from "../updateText";

const updateTextSub = (plSymVal) => {
  updateText(plSymVal);
  //   const plSymVal = localStorage.getItem("playerSymbol");
  //   return plSymVal;
};

export default updateTextSub;
